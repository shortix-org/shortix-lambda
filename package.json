{
  "name": "shortix-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "package": "npm run build && mkdir -p dist/nodejs && cp -r layer/nodejs/node_modules dist/nodejs/ && cd dist && zip -r ../layer.zip nodejs && zip -r ../backend.zip handlers shared"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.0.3",
    "aws-sdk": "^2.1693.0",
    "aws-sdk-mock": "^6.2.1",
    "jest": "^30.2.0",
    "nanoid": "^3.3.6",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "nanoid": "^3.3.6"
  }
}